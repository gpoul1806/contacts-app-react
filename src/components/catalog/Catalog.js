import React from "react";
import "./Catalog.scss";
import Contacts from "../contacts";

const Catalog = () => {
  return (
    <div className="catalog">
      <Contacts />
    </div>
  );
};

export default Catalog;
